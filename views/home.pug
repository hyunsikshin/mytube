extends layouts/main
include mixins/videoBlock

block content
  .video
    each video in videos
      +videoBlock({title: video.title,
      id: video.id,
      views: video.views,
      videoFile: video.fileUrl})